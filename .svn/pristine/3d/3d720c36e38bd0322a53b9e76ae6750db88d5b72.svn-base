name: lanecn
version: 1
cron:
handle:

- rewrite: if(path ~ "^(.*)$"  && !is_file() && !is_dir()) goto "index.php$1"
- rewrite: if(path ~ "^admin.php/(.*)$"  && !is_file() && !is_dir()) goto "admin.php$1"
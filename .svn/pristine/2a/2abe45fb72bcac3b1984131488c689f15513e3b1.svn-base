---
name: lanecn
version: 1
...

cron:

handle:
 - rewrite: if(path ~ "^(.*)$"  && !is_file() && !is_dir()) goto "index.php"
 - rewrite: if(path ~ "^admin/(.*)$"  && !is_file() && !is_dir()) goto "index.php"
